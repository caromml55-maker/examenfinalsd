apiVersion: v1
kind: Service
metadata:
  name: inventario-service-bg
spec:
  type: NodePort
  selector:
    app: inventario-app
    version: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000