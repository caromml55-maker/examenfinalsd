apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventario-app-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventario-app
      version: blue
  template:
    metadata:
      labels:
        app: inventario-app
        version: blue
    spec:
      containers:
      - name: inventario-app
        image: ghcr.io/caromml55-maker/examenfinalsd:latest
        imagePullPolicy: Always
        env:
        - name: APP_VERSION
          value: "v1"
        - name: APP_COLOR
          value: "blue"
        ports:
        - containerPort: 3000
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
